import Modal from "../"

const ServiceModal = ({ onClose }: { onClose: () => void }) => {
    return (
        <Modal onClose={onClose}>
            <div>
                
            </div>
        </Modal>
    )
}

export default ServiceModal
